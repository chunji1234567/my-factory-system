asgiref==3.11.1
Django==6.0.3
django-cors-headers==4.9.0
djangorestframework==3.17.0
pillow==12.1.1
sqlparse==0.5.5
djangorestframework-simplejwt==5.3.1
django-filter==24.2
python-dotenv==1.0.1
dj-database-url==2.2.0
# 发货单 PDF 生成（详见 business/api/shipping_note_pdf.py）。
# 选 reportlab 而不是 WeasyPrint 是因为它纯 Python、无原生依赖
# （cairo/pango），Windows/Linux/macOS 都能 pip 装上。
# 中文用 STSong-Light CID 字体，不需要额外打包 TTF。
reportlab==4.2.5
# 财务台账 Excel 导出（详见 business/api/views.py FinancePartnerLedgerExportView）。
# 2026-06-19 起从 CSV 切换到 xlsx——需要合并单元格、列宽自适应、单元格样式。 
openpyxl==3.1.5
